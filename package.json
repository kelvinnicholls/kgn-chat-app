{
  "name": "node-chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "export NODE_ENV=test || set \"NODE_ENV=test\" && mocha server/**/*.test.js --timeout=5000",
    "start": "node server/server.js",
    "mon": "nodemon server/server.js",
    "test-watch": "nodemon --exec \"npm test\""
  },
  "engines": {
    "node": "6.10.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2"
  }
}